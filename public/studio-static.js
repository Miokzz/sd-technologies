const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;
const nav=document.getElementById('sdNav'); const onScroll=()=>nav.classList.toggle('is-scrolled',scrollY>24); onScroll(); addEventListener('scroll',onScroll,{passive:true});
const menuBtn=document.getElementById('menuBtn'), mobile=document.getElementById('mobileMenu');
menuBtn.addEventListener('click',()=>{const open=mobile.hidden; mobile.hidden=!open; menuBtn.setAttribute('aria-expanded',String(open)); menuBtn.setAttribute('aria-label',open?'Fechar menu':'Abrir menu')});
mobile.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{mobile.hidden=true;menuBtn.setAttribute('aria-expanded','false')}));
if(!reduce&&'IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -5% 0px'});document.querySelectorAll('.reveal').forEach(e=>io.observe(e))}else document.querySelectorAll('.reveal').forEach(e=>e.classList.add('in'));
const words=['presença','experiência','produto','tecnologia'];let wi=0;setInterval(()=>{wi=(wi+1)%words.length;const el=document.getElementById('slotWord');el.animate([{transform:'translateY(70%)',opacity:0},{transform:'translateY(0)',opacity:1}],{duration:380,easing:'cubic-bezier(.22,1,.36,1)'});el.textContent=words[wi]},2200);
const serviceData=[["Sites institucionais", "Presença digital", "Sites completos, rápidos e claros para apresentar sua empresa com autoridade e transformar visita em contato."], ["Landing pages", "Conversão", "Páginas focadas em campanhas, lançamentos e serviços, com narrativa enxuta e chamadas pensadas para gerar ação."], ["Redesign", "Modernização", "Reestruturamos experiências antigas sem apagar a identidade do negócio, melhorando clareza, estética e desempenho."], ["Negócios locais", "Descoberta", "Estrutura digital preparada para destacar localização, diferenciais, canais de contato e serviços de quem atende localmente."], ["Portfólios & catálogos", "Apresentação", "Arquiteturas visuais para mostrar trabalhos, produtos e serviços sem transformar tudo em uma parede de cards."], ["Integrações", "Conexão", "WhatsApp, formulários, mapas e pontos de contato integrados ao fluxo do site para diminuir atrito."]];
document.querySelectorAll('[data-service]').forEach(btn=>btn.addEventListener('mouseenter',()=>activateService(btn)).addEventListener('focus',()=>activateService(btn)).addEventListener('click',()=>activateService(btn)));
function activateService(btn){const i=Number(btn.dataset.service);document.querySelectorAll('[data-service]').forEach(x=>x.classList.remove('is-active'));btn.classList.add('is-active');document.getElementById('serviceCount').textContent=String(i+1).padStart(2,'0')+'/06';document.getElementById('serviceMeta').textContent=serviceData[i][1];document.getElementById('serviceTitle').textContent=serviceData[i][0];document.getElementById('serviceDesc').textContent=serviceData[i][2];document.getElementById('serviceStage').className='sd-service-stage stage-'+(i+1)}
document.querySelectorAll('[data-faq]').forEach(item=>{const b=item.querySelector('button'),a=item.querySelector('.sd-faq-answer'),icon=b.querySelector('i');b.addEventListener('click',()=>{const open=b.getAttribute('aria-expanded')==='true';b.setAttribute('aria-expanded',String(!open));item.classList.toggle('is-open',!open);a.style.display=open?'none':'block';icon.textContent=open?'+':'−'})})
const form=document.getElementById('briefing'),status=document.getElementById('formStatus');form.addEventListener('submit',e=>{e.preventDefault();if(!form.reportValidity())return;status.textContent='Briefing validado. O canal de recebimento será conectado ao contato oficial da S&D.'});
const hero=document.querySelector('.sd-hero'), heroMark=document.getElementById('heroMark');if(!reduce&&matchMedia('(hover:hover) and (pointer:fine)').matches){hero.addEventListener('pointermove',e=>{const r=hero.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;heroMark.style.transform=`translate3d(${x*16}px,${y*12}px,0)`},{passive:true});hero.addEventListener('pointerleave',()=>heroMark.style.transform='')}
if(!reduce){const c=document.getElementById('ribbon'),ctx=c.getContext('2d');let w=0,h=0,t=0,px=.5,py=.5;const rs=()=>{const d=Math.min(devicePixelRatio||1,2),r=c.getBoundingClientRect();w=r.width;h=r.height;c.width=w*d;c.height=h*d;ctx.setTransform(d,0,0,d,0,0)};rs();addEventListener('resize',rs);c.addEventListener('pointermove',e=>{const r=c.getBoundingClientRect();px=(e.clientX-r.left)/r.width;py=(e.clientY-r.top)/r.height},{passive:true});(function draw(){t+=.006;ctx.clearRect(0,0,w,h);const base=Math.min(w,h);for(let b=0;b<8;b++){ctx.beginPath();ctx.strokeStyle=`rgba(22,101,255,${.09+b*.018})`;ctx.lineWidth=b%3===0?1.5:1;for(let x=-40;x<=w+40;x+=12){const n=x/Math.max(w,1),wave=Math.sin(n*Math.PI*2.3+t*(1.1+b*.04)+b*.7),wave2=Math.sin(n*Math.PI*5.1-t*.7+b*.4)*.24,at=(px-.5)*base*.08*(n-.5),yp=(py-.5)*base*.035,y=h*(.25+b*.07)+wave*base*(.045+b*.003)+wave2*base*.03+at+yp;x===-40?ctx.moveTo(x,y):ctx.lineTo(x,y)}ctx.stroke()}requestAnimationFrame(draw)})()}